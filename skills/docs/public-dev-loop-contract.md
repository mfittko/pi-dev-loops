# Public dev-loop contract (packaged summary)

This packaged skill doc is a thin summary for installed-skill routing.

Canonical semantic owner:

- `docs/public-dev-loop-contract.md`

Use that source doc for the full public `dev-loop` contract semantics (entrypoint, routing, shorthand, gate behavior, and taxonomy).

This packaged copy is intentionally non-canonical and should stay as a pointer/summary rather than a second semantic owner.
